# 📅 2026-01-24 공부 기록
# 주제: OverTheWire Bandit (Level 12 → 13)

---

## 1. 문제 목표
data.txt 파일은 반복적으로 압축된 파일을 16진수(Hexdump)로 변환해 둔 것임.
이걸 다시 원본으로 돌리고, 수차례 걸려 있는 압축을 모두 풀어서 비밀번호를 찾아야 함.

## 2. 접근 방법
홈 디렉토리에는 파일을 쓸 수 없으니 /tmp 경로에 나만의 작업 폴더를 만들어서 진행함.
xxd 명령어로 헥사 코드를 바이너리 파일로 복구한 뒤, file 명령어로 압축 형식을 확인해가며 껍질을 하나씩 까는 방식으로 접근함.

## 3. 풀이 과정 (핵심 명령어)
```bash
# 1. 임시 폴더 생성 및 파일 복사
mkdir /tmp/yechan_work
cp data.txt /tmp/yechan_work/
cd /tmp/yechan_work/

# 2. 헥사코드 복구
xxd -r data.txt > data_restored

# 3. 반복 압축 해제 (file 명령어로 확인 후 반복)
# gzip, bzip2, tar가 섞여 있어서 상황에 맞게 계속 풀었음
file data_restored
mv data_restored data.gz
gzip -d data.gz
