# 📅 2026-01-21 공부 기록
# 주제: OverTheWire Bandit (Level 9 → 10)

---

## 1. 문제 목표
data.txt 파일 안에 숨겨진 비밀번호를 찾아야 함.
비밀번호는 사람이 읽을 수 있는 문자열 형태이고, 앞에 등호(=)가 여러 개 붙어있다는 조건이 있음.

## 2. 접근 방법
처음에는 Level 8이랑 비슷해 보여서 바로 grep을 쓰거나 cat으로 열어보려고 했음.
하지만 확인해 보니 data.txt가 일반 텍스트 파일이 아니라 바이너리 파일이었음.
기계어 데이터 속에 섞여 있는 문자열만 추출하는 방법이 필요해서 구글링을 통해 해결책을 찾음.

## 3. 풀이 명령어
```bash
strings data.txt | grep "===="
